package com.janvoice.ai.entity;

/**
 * Enumeration representing the urgency levels of Reported Complaints,
 * ranging from LOW to CRITICAL (typically derived by Google Gemini AI
 * analyses).
 */
public enum Urgency {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
